<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - CSE Motors</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        .users-table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        .users-table th, .users-table td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
        }
        .users-table th {
            background-color: #f4f4f4;
            font-weight: bold;
        }
        .users-table tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        .users-table tr:hover {
            background-color: #f5f5f5;
        }
        .account-type-form {
            display: inline;
        }
        .badge {
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 12px;
            font-weight: bold;
        }
        .badge-admin { background-color: #dc3545; color: white; }
        .badge-employee { background-color: #007bff; color: white; }
        .badge-client { background-color: #28a745; color: white; }
    </style>
</head>
<body>
    <%- include('../partials/header') %>

    <main class="container">
        <h1><%= title %></h1>
        <p class="subtitle">Welcome, Admin <%= currentUser.account_firstname %></p>
        
        <!-- Flash Messages -->
        <% if (messages && messages.error) { %>
            <div class="flash-message error">
                <%= messages.error %>
            </div>
        <% } %>
        <% if (messages && messages.success) { %>
            <div class="flash-message success">
                <%= messages.success %>
            </div>
        <% } %>

        <div class="admin-actions">
            <a href="/inv/management" class="btn btn-primary">Manage Inventory</a>
            <a href="/account/" class="btn btn-secondary">My Account</a>
        </div>

        <div class="users-section">
            <h2>Registered Users (<%= accounts.length %>)</h2>
            
            <% if (accounts.length === 0) { %>
                <p>No users found.</p>
            <% } else { %>
                <table class="users-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Account Type</th>
                            <th>Joined</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% accounts.forEach(function(user) { %>
                            <tr>
                                <td><%= user.account_id %></td>
                                <td><%= user.account_firstname %> <%= user.account_lastname %></td>
                                <td><%= user.account_email %></td>
                                <td>
                                    <span class="badge badge-<%= user.account_type.toLowerCase() %>">
                                        <%= user.account_type %>
                                    </span>
                                </td>
                                <td><%= new Date(user.account_created).toLocaleDateString() %></td>
                                <td>
                                    <% if (user.account_id !== currentUser.account_id) { %>
                                        <form action="/account/update-type" method="post" class="account-type-form">
                                            <input type="hidden" name="target_account_id" value="<%= user.account_id %>">
                                            <select name="new_account_type" onchange="this.form.submit()">
                                                <option value="Client" <%= user.account_type === 'Client' ? 'selected' : '' %>>Client</option>
                                                <option value="Employee" <%= user.account_type === 'Employee' ? 'selected' : '' %>>Employee</option>
                                                <option value="Admin" <%= user.account_type === 'Admin' ? 'selected' : '' %>>Admin</option>
                                            </select>
                                        </form>
                                    <% } else { %>
                                        <em>(Current User)</em>
                                    <% } %>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } %>
        </div>

        <div class="admin-notes">
            <h3>Admin Notes</h3>
            <ul>
                <li>Click on any dropdown to change a user's account type</li>
                <li>Admins have full access to all system features</li>
                <li>Employees can manage inventory but not user accounts</li>
                <li>Clients have basic access to view vehicles and manage their profile</li>
                <li>You cannot change your own account type from this panel</li>
            </ul>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>